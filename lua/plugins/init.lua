-- =========================================================
-- Plugin spec entry
-- =========================================================
return {
  require("plugins.colorscheme"),
  require("plugins.neo-tree"),
  require("plugins.indent"),
  require("plugins.telescope"),

  -- Optional: git related (enable when you need it)
  -- require("plugins.git"),
}
